clase lexico
dividir la linea de entrada entre
comoponentes(componente definidos en componentes) 
el comportamiento de objetos es:
al crear el objeto se le pasa la cadena a analizar, leida por teclado
luego se llama al metodo :siguiente q devolvera en cada llamada
un nuevo componente lexico (espacios en blanco se omiten)
El metodo siguiente sin parametros, al ser llamado, devuelve el siguiente 
componente en la cadena q no haya q omitir
el metodo siguiente puede diseñarse como una MDD o como:
(ver pdf)

escribir componentes lexicos, añadir por objeto metrodo __str__
